{
  "name": "myportfolio1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8080 --entry-file=index.html --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
